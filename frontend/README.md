# Frontend Quick Start

## 1) 环境变量

```bash
cp .env.example .env
```

根据你的后端地址修改 `VITE_API_BASE_URL`。

## 2) 安装与启动

```bash
npm install
npm run dev
```

默认访问：http://127.0.0.1:5173

## 3) 当前页面

- `/` 炫酷首页（含供应商旋转发散组件、注册/登录/控制台入口）
- `/login` 占位
- `/register` 占位
- `/console` 占位

